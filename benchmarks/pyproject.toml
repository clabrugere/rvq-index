[project]
name = "rvq-benchmarks"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "torch>=2.5",
    "sentence-transformers>=3.3",
    "safetensors>=0.4",
    "numpy",
    "datasets",
    "huggingface_hub",
]
